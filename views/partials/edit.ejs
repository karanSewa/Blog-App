<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Post</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%- include("header.ejs") %>

    <div class="container blog-textContainer">
      <p class="text-center text-danger fs-1">Edit Post</p>
      <div class="main text-center">
        <% if(locals.post) {%>
        <form class="border-2" action="/edit/<%= id %>" method="POST" autocomplete="off">
          <div class="container form-container">
            <div class="blogHeadContainer">
              <label class="fs-3 text-danger mb-2" for="blogHeading">tittle</label>
              <br />
              <input class="rounded-2 border border-danger border-2" type="text" name="blogHeading" id="blogHeading" value="<%= post.title %>" required />
            </div>
            <div class="blogTextarea">
              <label class="fs-3 text-danger mb-2" for="blogText">post</label>
              <br />
              <textarea class="rounded-2 border border-danger border-2" type="text" name="blogText" id="blogText" required ><%= post.content %></textarea>
            </div>
            <div class="blogButton">
              <button class="btn btn-dark btn-lg" type="submit" id="submit" value="edit">
                Save Post
              </button>
               <% } %>
            </div>
          </div>
        </form>
      </div>
    </div>

    <%- include("footer.ejs") %>
    <script src="app.js"></script>
  </body>
</html>
