<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Post</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%- include("header.ejs") %>
    <section id="edit" class="container-fluid">
  
      <h2 class="text">Edit Post</h2>
        <% if(locals.post) {%>
          <div class="form-container container">
            <form action="/edit/<%= id %>" method="POST" autocomplete="off">
                <div class="blogHeadContainer">
                  <label for="blogHeading">tittle</label>
                  <br />
                  <input type="text" name="blogHeading" id="blogHeading" value="<%= post.title %>" required />
                </div>
                <div class="blogTextarea">
                  <label for="blogText">post</label>
                  <br />
                  <textarea type="text" name="blogText" id="blogText" required ><%= post.content %></textarea>
                </div>
                  <button class="btn btn-outline-danger" type="submit" id="submit" value="edit">
                    Save Post
                  </button>
                  <% } %>   
           </form>
          </div>
  </section>
    <%- include("footer.ejs") %>
    <script src="app.js"></script>
  </body>
</html>
