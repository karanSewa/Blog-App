<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Post</title>
    <link rel="stylesheet" href="styles/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%- include("header.ejs") %>

    <div class="container blog-textContainer">
      <p
        class="text-center text-danger fs-1 border border-2 rounded-2 bg-primary"
      >
        New
      </p>

      <form
        class="border-2 rounded-2 bg-primary"
        action="/post"
        method="POST"
        autocomplete="off"
      >
        <div class="container form-container">
          <div class="blogHeadContainer">
            <input
              class="rounded-2 border border-dark border-4"
              type="text"
              name="blogHeading"
              id="blogHeading"
              required
            />
          </div>
          <div class="blogTextarea">
            <textarea
              class="rounded-2 border border-dark border-4"
              type="text"
              name="blogText"
              id="blogText"
              required
            ></textarea>
          </div>
          <div class="blogButton">
            <button
              class="btn btn-dark btn-lg"
              type="submit"
              id="submit"
              value="submit"
            >
              Submit
            </button>
          </div>
        </div>
      </form>
    </div>

    <%- include("footer.ejs") %>
    <script src="app.js"></script>
  </body>
</html>
